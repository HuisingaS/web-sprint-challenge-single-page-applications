import React from 'react';

function Customer(props) {
    const {
        name,
        size,
        toppings,
        specialOrder
    } = props

    return (
        <div>
            <h1>CustomerTest</h1>
        </div>
    )
};

export default Customer;